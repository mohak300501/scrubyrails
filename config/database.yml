default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: scrubyrails_development
  username: sanskritclub
  password: namaskar
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: myapp_test
  username: sanskritclub
  password: namaskar
  host: localhost

production:
  <<: *default
  database: <%= Rails.application.credentials.config[:database][:DATABASE] %>
  username: <%= Rails.application.credentials.config[:database][:DATABASE_USERNAME] %>
  password: <%= Rails.application.credentials.config[:database][:DATABASE_PASSWORD] %>
