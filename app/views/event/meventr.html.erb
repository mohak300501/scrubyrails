<div class="admin-events-page">
  <div class="admin-header">
    <h1>घटना-प्रबन्धनम्</h1>
    <p class="admin-subtitle">सर्वे घटनाः सम्पादयतु</p>
    <%= link_to new_event_path, class: "btn btn-create" do %>
      <span class="btn-icon">➕</span>
      नूतना घटना योजयतु
    <% end %>
  </div>

  <div class="admin-events-container">
    <% @events.each do |event| %>
      <div class="admin-event-card">
        <div class="admin-event-image">
          <%= image_tag rails_blob_url(event.image), alt: event.name %>
        </div>
        
        <div class="admin-event-content">
          <h2 class="admin-event-title"><%= event.name %></h2>
          <div class="admin-event-info">
            <%= truncate(event.info.html_safe, length: 150) %>
          </div>
          
          <div class="admin-event-actions">
            <%= link_to event_path(event.id), class: "btn btn-edit" do %>
              <span class="btn-icon">✏️</span>
              परिवर्तयतु
            <% end %>
            <%= button_to event_delete_path(event.id),
            method: :delete,
            data: { confirm: "किं भवान् निश्चितः अस्ति यत् एतं घटनां विनाशयितुम् इच्छति?" },
            class: "btn btn-delete" do %>
          <span class="btn-icon">🗑️</span>
          विनश्यतु
        <% end %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>