<div class="sw-admin">
  <div class="sw-admin-header">
    <h1>Add Photos — <%= @past_event.title %></h1>
    <%= link_to "← Back to Photos", sw_past_photos_path(@past_event), class: "sw-admin-link" %>
  </div>
  <%= render 'form', photo: SanskritweekPhoto.new %>
</div>