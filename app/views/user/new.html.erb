<h1>Sign up as new user</h1>
<% if !session[:otp] %>
    <%= form_tag newp_path do %>
        <div>
            <%= text_field_tag :name, nil, :required => true, placeholder: "Your name" %><br/>
            <%= email_field_tag :email, nil, :required => true, placeholder: "Your email" %><br/>
            <%= password_field_tag :password, nil, :required => true, placeholder: "Set password" %><br/>
            <%= password_field_tag :repass, nil, :required, placeholder: "Re-enter password" => true %>
        </div>
        <div>
            <%= submit_tag "Submit", :class => "button" %>
        </div>
    <% end %>
<% else %>
    <%= form_tag otpp_path do %>
        <%= text_field_tag :otp, nil, :required => true, placeholder: "Enter OTP" %><br/>
        <%= submit_tag "Submit", :class => "button" %>
    <% end %>
<% end %>
<br/>
<% flash.each do |type, msg| %>
    <div class="flash">
        <%= msg %>
    </div>
<% end %>
<br/>
Already signed up? Proceed to <%= link_to "Login", user_login_path %>.